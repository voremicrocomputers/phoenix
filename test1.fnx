fn main() {
    let strvar: &char = "hello world";
    {
        let strvar: &char = "hi";
        puts(strvar);
        let strvar: &char = "never prints";
    }
    puts(strvar);
    
    let input_value: &char = get_string();
    puts("the input value was:");
    puts(input_value);
}